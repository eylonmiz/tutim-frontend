import React from "react"

interface ChatbotErrorProps {
  message: string
}

const ChatbotError = ({ message }: ChatbotErrorProps) => {
  return <div>ChatbotError</div>
}

export default ChatbotError
